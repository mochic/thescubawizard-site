import React, { useRef, useState } from "react"

import { animated, useSpring, useChain, config } from "react-spring"

import styled from "styled-components"

// dont think about pages just think about sections to be revealed
const Schedule = () => {}
