import styled from "styled-components"
import { animated } from "react-spring"

export const AContainr = styled(animated.div)``

export const AH1 = styled(animated.h1)``

export const AH2 = styled(animated.h2)``
